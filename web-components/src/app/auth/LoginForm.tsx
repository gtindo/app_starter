import { h } from "preact";
import { define } from "preactement";


export function LoginForm() {
  return (
    <div>Login form component component</div>
  )
}

define('app-login-form', () => LoginForm);